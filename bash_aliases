# ~/.bash_aliases
# Some custom bash aliases for bash

# clear screen
alias c='clear'

# ls modifications
alias lt='ls --human-readable --size -l -S --classify'

# git related aliases
alias gs='git status'
